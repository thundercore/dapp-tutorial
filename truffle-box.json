{
  "ignore": [],
  "commands": {
    "Compile contracts": "truffle compile",
    "Migrate contracts": "truffle migrate",
    "Test contracts": "truffle test"
  },
  "hooks": {
    "post-unpack": "npm install; truffle compile; cd client/src && ln -s ../../build/contracts contracts && cd .. && npm install;"
  }
}
